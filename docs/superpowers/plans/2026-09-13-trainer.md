# Тренажёр — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Тренажёр главы 1 (7 класс) с режимами Тренировка и Зачёт на GH Pages
к занятию 30.09.2026, плюс хвосты: белый список публикации, дата прохождения
в course-map.json, живая ссылка на лендинге.

**Architecture:** Статика vanilla ES-модулей в `trainer/` (engine.js — чистая
логика, ui.js — DOM, storage.js — localStorage), банк вопросов —
`textbook/quizzes/*.json` по файлу на параграф + рукописный манифест
`index.json`, читается `fetch`-ем. Валидация банка — Python-скрипт в CI перед
сборкой сайта.

**Tech Stack:** Vanilla JS (ES-модули, без сборки), `node --test` (node ≥18,
локально v26), Python 3 stdlib (валидатор), pytest в `tools/.venv` (тесты
инструментов), rsync (сборка сайта), GitHub Actions (деплой).

**Spec:** `docs/superpowers/specs/2026-09-13-trainer-design.md` — читать до
начала работ. Контекст банка: OCR учебника в
`~/Library/Caches/junior_it_ocr/7kl.txt` (маркеры `=== PAGE p-NNN ===`).

## Global Constraints

- Язык материалов и UI — русский; идентификаторы, имена файлов, ключи JSON —
  английские.
- Vanilla HTML/CSS/JS, без фреймворков и без шага сборки.
- Цвета и шрифты — только из `meta/brand-tokens.css`; акцент тренажёра —
  `--track-book: #7aa2ff`. Шрифты Rubik 700 / Manrope / JetBrains Mono
  (Google Fonts, сабсет cyrillic у всех трёх проверен).
- Без эмодзи в коде, коммитах и идентификаторах.
- Комментарии в коде — только для неочевидного.
- Правки существующих файлов — хирургические.
- Поле `why` обязательно у каждого вопроса банка.
- Никаких таймеров, очков за скорость, стриков.
- Коммиты небольшие, после каждой задачи. `git push` не делать — деплой
  запускает Димас через `/deploy`.

---

### Task 1: Валидатор банка вопросов

**Files:**
- Create: `tools/validate_quizzes.py`
- Test: `tools/tests/test_validate_quizzes.py`

**Interfaces:**
- Produces: CLI `python3 tools/validate_quizzes.py <repo_root>` — exit 0 если
  банк валиден, exit 1 + список ошибок в stderr. Функция
  `validate(repo_root: Path) -> list[str]` (пустой список = ок) — для тестов.
- Consumes: `textbook/quizzes/index.json` и файлы банка (формат ниже — он же
  контракт для Task 2, 3 и движка).

Формат манифеста `textbook/quizzes/index.json`:

```json
{
  "banks": [
    {
      "id": "07-1",
      "grade": 7,
      "chapter": 1,
      "title": "Информация и информационные процессы",
      "files": ["07-1.1.json", "07-1.2.json", "07-1.3.json", "07-1.4.json", "07-1.5.json"]
    }
  ]
}
```

Формат файла параграфа:

```json
{
  "grade": 7,
  "chapter": 1,
  "paragraph": "1.1",
  "title": "Информация и данные",
  "questions": [
    {
      "id": "07-1-01",
      "source": "7 кл, гл. 1, тест 1",
      "type": "single",
      "q": "Текст вопроса",
      "options": ["вариант а", "вариант б"],
      "answer": 0,
      "why": "Объяснение, почему так.",
      "terms": [{"ru": "бит", "en": "bit"}]
    }
  ]
}
```

Правила по типам (`options`/`answer`):

| type | options | answer |
|---|---|---|
| `single`, `find-error` | список строк, ≥2 | int в диапазоне options |
| `multi` | список строк, ≥2 | список int, ≥1, без дублей, в диапазоне |
| `number` | отсутствует | int или float |
| `text` | отсутствует | непустая строка или список непустых строк |
| `match` | `{"left": [...], "right": [...]}`, left ≥2 | список int длиной len(left), каждый в диапазоне right |
| `order` | список строк, ≥2 | перестановка `range(len(options))` |

Общие проверки: `id`, `source`, `q`, `why` — непустые строки; `type` из семи;
`terms` — список объектов `{ru, en}` (может быть пустым); id уникальны в
пределах банка (главы); шапка файла обязательна, `paragraph` начинается с
номера главы (`1.` для chapter 1); в каждом файле ≥3 вопросов; каждый файл из
манифеста существует, лишних `07-*.json` вне манифеста нет.

- [ ] **Step 1: Написать падающие тесты**

`tools/tests/test_validate_quizzes.py` (стиль соседних тестов — fixtures на
`tmp_path`):

```python
import json
from pathlib import Path

from validate_quizzes import validate

BANK_DIR = "textbook/quizzes"


def q(qid="07-1-01", qtype="single", **over):
    base = {
        "id": qid, "source": "7 кл, гл. 1, тест 1", "type": qtype,
        "q": "Вопрос?", "options": ["а", "б", "в"], "answer": 0,
        "why": "Потому.", "terms": [],
    }
    base.update(over)
    return base


def write_bank(root, files):
    d = root / BANK_DIR
    d.mkdir(parents=True)
    manifest = {"banks": [{
        "id": "07-1", "grade": 7, "chapter": 1, "title": "Глава 1",
        "files": sorted(files),
    }]}
    (d / "index.json").write_text(
        json.dumps(manifest, ensure_ascii=False), encoding="utf-8")
    for name, questions in files.items():
        payload = {"grade": 7, "chapter": 1,
                   "paragraph": name.removeprefix("07-").removesuffix(".json"),
                   "title": "Параграф", "questions": questions}
        (d / name).write_text(
            json.dumps(payload, ensure_ascii=False), encoding="utf-8")


def three(prefix):
    return [q(f"{prefix}-{i}") for i in range(3)]


def test_valid_bank_passes(tmp_path):
    write_bank(tmp_path, {"07-1.1.json": three("07-1")})
    assert validate(tmp_path) == []


def test_empty_why_fails(tmp_path):
    bad = three("07-1")
    bad[0]["why"] = "  "
    write_bank(tmp_path, {"07-1.1.json": bad})
    assert any("why" in e for e in validate(tmp_path))


def test_answer_out_of_range_fails(tmp_path):
    bad = three("07-1")
    bad[1]["answer"] = 3
    write_bank(tmp_path, {"07-1.1.json": bad})
    assert any("answer" in e for e in validate(tmp_path))


def test_duplicate_ids_across_files_fail(tmp_path):
    write_bank(tmp_path, {"07-1.1.json": three("07-1"),
                          "07-1.2.json": three("07-1")})
    assert any("id" in e for e in validate(tmp_path))


def test_fewer_than_three_questions_fail(tmp_path):
    write_bank(tmp_path, {"07-1.1.json": three("07-1")[:2]})
    assert any("меньше 3" in e for e in validate(tmp_path))


def test_missing_file_from_manifest_fails(tmp_path):
    write_bank(tmp_path, {"07-1.1.json": three("07-1")})
    (tmp_path / BANK_DIR / "07-1.1.json").unlink()
    assert validate(tmp_path)


def test_order_answer_must_be_permutation(tmp_path):
    bad = three("07-1")
    bad[2] = q("07-1-2", "order", options=["а", "б", "в"], answer=[0, 0, 1])
    write_bank(tmp_path, {"07-1.1.json": bad})
    assert any("перестановк" in e for e in validate(tmp_path))


def test_match_answer_length_and_range(tmp_path):
    bad = three("07-1")
    bad[2] = q("07-1-2", "match",
               options={"left": ["а", "б"], "right": ["x", "y", "z"]},
               answer=[0, 5])
    write_bank(tmp_path, {"07-1.1.json": bad})
    assert validate(tmp_path)
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `cd tools && .venv/bin/python -m pytest tests/test_validate_quizzes.py -v`
Expected: FAIL — `ModuleNotFoundError: validate_quizzes`

- [ ] **Step 3: Реализовать `tools/validate_quizzes.py`**

Чистый stdlib (`json`, `pathlib`, `sys`). Структура:

```python
#!/usr/bin/env python3
"""Валидатор банка вопросов тренажёра: textbook/quizzes/*.json.

Схема — docs/superpowers/specs/2026-09-13-trainer-design.md. Ошибки
собираются списком, а не бросаются: одна прогонка показывает всё сразу.
"""

import json
import sys
from pathlib import Path

TYPES = {"single", "multi", "number", "text", "match", "order", "find-error"}
MIN_QUESTIONS = 3


def _is_str_list(value, minimum=2):
    return (isinstance(value, list) and len(value) >= minimum
            and all(isinstance(v, str) and v.strip() for v in value))


def _check_question(q, path, errors):
    qid = q.get("id", "?")
    for field in ("id", "source", "q", "why"):
        if not isinstance(q.get(field), str) or not q[field].strip():
            errors.append(f"{path}: вопрос {qid}: пустое поле {field}")
    if not isinstance(q.get("terms"), list) or not all(
            isinstance(t, dict) and t.get("ru") and t.get("en")
            for t in q.get("terms", [])):
        errors.append(f"{path}: вопрос {qid}: terms — список пар ru/en")
    qtype = q.get("type")
    options, answer = q.get("options"), q.get("answer")
    if qtype not in TYPES:
        errors.append(f"{path}: вопрос {qid}: неизвестный type {qtype!r}")
    elif qtype in ("single", "find-error"):
        if not _is_str_list(options):
            errors.append(f"{path}: вопрос {qid}: options — список строк, минимум 2")
        elif not (isinstance(answer, int) and 0 <= answer < len(options)):
            errors.append(f"{path}: вопрос {qid}: answer вне диапазона options")
    elif qtype == "multi":
        if not _is_str_list(options):
            errors.append(f"{path}: вопрос {qid}: options — список строк, минимум 2")
        elif (not isinstance(answer, list) or not answer
              or len(set(answer)) != len(answer)
              or not all(isinstance(a, int) and 0 <= a < len(options) for a in answer)):
            errors.append(f"{path}: вопрос {qid}: answer — список индексов без дублей в диапазоне options")
    elif qtype == "number":
        if options is not None:
            errors.append(f"{path}: вопрос {qid}: у number не бывает options")
        if not isinstance(answer, (int, float)) or isinstance(answer, bool):
            errors.append(f"{path}: вопрос {qid}: answer у number — число")
    elif qtype == "text":
        if options is not None:
            errors.append(f"{path}: вопрос {qid}: у text не бывает options")
        accepted = answer if isinstance(answer, list) else [answer]
        if not _is_str_list(accepted, minimum=1):
            errors.append(f"{path}: вопрос {qid}: answer у text — непустая строка или их список")
    elif qtype == "match":
        left = options.get("left") if isinstance(options, dict) else None
        right = options.get("right") if isinstance(options, dict) else None
        if not _is_str_list(left) or not _is_str_list(right):
            errors.append(f"{path}: вопрос {qid}: options у match — left и right, минимум по 2")
        elif (not isinstance(answer, list) or len(answer) != len(left)
              or not all(isinstance(a, int) and 0 <= a < len(right) for a in answer)):
            errors.append(f"{path}: вопрос {qid}: answer у match — индекс right на каждый left")
    elif qtype == "order":
        if not _is_str_list(options):
            errors.append(f"{path}: вопрос {qid}: options — список строк, минимум 2")
        elif not (isinstance(answer, list) and sorted(answer) == list(range(len(options)))):
            errors.append(f"{path}: вопрос {qid}: answer у order — перестановка индексов options")


def validate(repo_root):
    errors = []
    bank_dir = Path(repo_root) / "textbook" / "quizzes"
    manifest_path = bank_dir / "index.json"
    if not manifest_path.is_file():
        return [f"{manifest_path}: манифест не найден"]
    manifest = json.loads(manifest_path.read_text(encoding="utf-8"))
    listed = set()
    for bank in manifest.get("banks", []):
        seen_ids = {}
        for name in bank.get("files", []):
            listed.add(name)
            path = bank_dir / name
            if not path.is_file():
                errors.append(f"{path}: файл из манифеста не найден")
                continue
            data = json.loads(path.read_text(encoding="utf-8"))
            for field in ("grade", "chapter", "paragraph", "title"):
                if field not in data:
                    errors.append(f"{path}: в шапке нет поля {field}")
            if not str(data.get("paragraph", "")).startswith(f"{bank['chapter']}."):
                errors.append(f"{path}: paragraph не из главы {bank['chapter']}")
            questions = data.get("questions", [])
            if len(questions) < MIN_QUESTIONS:
                errors.append(f"{path}: меньше 3 вопросов — билет 3x5 не собрать")
            for q in questions:
                _check_question(q, path.name, errors)
                if q.get("id") in seen_ids:
                    errors.append(f"{path.name}: дубль id {q['id']} (уже в {seen_ids[q['id']]})")
                seen_ids[q.get("id")] = path.name
    stray = {p.name for p in bank_dir.glob("[0-9][0-9]-*.json")} - listed
    for name in sorted(stray):
        errors.append(f"{name}: файла нет в манифесте")
    return errors


def main(argv=None):
    args = sys.argv[1:] if argv is None else argv
    repo_root = Path(args[0]) if args else Path(__file__).resolve().parent.parent
    errors = validate(repo_root)
    for e in errors:
        print(f"quizzes: {e}", file=sys.stderr)
    if errors:
        return 1
    print("quizzes: банк валиден")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

Уникальность id в коде выше проверяется в пределах одного банка (`seen_ids`
на банк) — этого достаточно: id несут префикс главы.

- [ ] **Step 4: Прогнать тесты**

Run: `cd tools && .venv/bin/python -m pytest tests/test_validate_quizzes.py -v`
Expected: PASS (8 тестов)

- [ ] **Step 5: Прогнать весь набор tools, чтобы ничего не задеть**

Run: `cd tools && .venv/bin/python -m pytest`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add tools/validate_quizzes.py tools/tests/test_validate_quizzes.py
git commit -m "trainer: валидатор банка вопросов"
```

---

### Task 2: Банк главы 1 из учебника (черновик под вычитку)

**Files:**
- Create: `textbook/quizzes/index.json`
- Create: `textbook/quizzes/07-1.1.json` … `07-1.5.json`
- Create: `textbook/quizzes/REVIEW-07-1.md` (временный, для вычитки Димаса —
  удаляется после вычитки)

**Interfaces:**
- Consumes: формат из Task 1; OCR `~/Library/Caches/junior_it_ocr/7kl.txt`.
- Produces: валидный банк из 20 вопросов учебника, распределённых по 5
  файлам параграфов.

Источники в OCR (номера строк ориентировочные — сверяться по маркерам
страниц):

- Тестовые задания главы 1: строки ~2083–2330 (страницы p-043…p-047),
  сквозная нумерация 1–20, идут после §1.5.
- Ключ (глава 1, с. 244, строки ~12747–12760):
  `1-в 2-б 3-а 4-в 5-б 6-г 7-б 8-в 9-г 10-б 11-в 12-б 13-б 14-а 15-в 16-в 17-г 18-в 19-г 20-а`.
- Названия параграфов — из `textbook/atlas-07.md`: 1.1 Информация и данные ·
  1.2 Информационные процессы · 1.3 Представление информации · 1.4 Двоичное
  представление данных · 1.5 Измерение информации (сверить с оглавлением в
  OCR, страницы p-250+).

- [ ] **Step 1: Извлечь 20 вопросов из OCR**

Прочитать блок тестовых заданий целиком. Типовые ошибки OCR, которые чинить
при переносе: `6)` → `б)`, `0б` → `об`, `LL.` → `11.`, `B` → `в`, `r` → `г`
в вариантах, латинские буквы внутри русских слов. Формулы и числа сверять
особо внимательно — OCR в них врёт.

- [ ] **Step 2: Распределить вопросы по параграфам**

В книге тесты к параграфам не привязаны — распределяем по содержанию:
сигналы/виды информации → 1.1, информационные процессы/носители → 1.2,
языки/кодирование → 1.3, двоичный код → 1.4, единицы измерения/объём → 1.5.
Каждое отнесение зафиксировать в `REVIEW-07-1.md` строкой
`тест N -> §X.Y (почему)`. Если параграф получил <3 вопросов — это нормально,
добор в Task 3.

- [ ] **Step 3: Собрать JSON-файлы**

Каждый вопрос: `id` `07-1-NN` (NN — сквозной номер теста, с ведущим нулём),
`source` `"7 кл, гл. 1, тест NN"`, `type` почти везде `single` (вопрос 11 —
про пять органов чувств, пять вариантов — остаётся `single`), `answer` — по
ключу, `why` — 1–2 предложения своими словами (не цитата учебника), `terms` —
двуязычные пары там, где в вопросе есть термин («бит (bit)», «сигнал
(signal)»…). Тексты вопросов и вариантов — дословно по учебнику, после
чистки OCR.

Манифест `index.json` — по формату из Task 1.

- [ ] **Step 4: Прогнать валидатор**

Run: `python3 tools/validate_quizzes.py .`
Expected: либо exit 0, либо только ошибки «меньше 3 вопросов» у параграфов,
которые доберёт Task 3 (это единственная допустимая красная ошибка на этом
шаге; остальное чинить сразу).

- [ ] **Step 5: Написать REVIEW-07-1.md**

Разделы: распределение по параграфам (из Step 2); список мест, где OCR был
сомнителен и что выбрано; счётчик вопросов по параграфам. Файл живёт до
вычитки, в деплой не попадает (textbook публикуется — поэтому после вычитки
удалить; до тех пор это черновик в репо, не страшно).

- [ ] **Step 6: Commit**

```bash
git add textbook/quizzes/
git commit -m "trainer: банк главы 1 — 20 вопросов учебника, черновик под вычитку"
```

---

### Task 3: Наши вопросы (source: junior_it)

**Files:**
- Modify: `textbook/quizzes/07-1.1.json` … `07-1.5.json` (добавить вопросы)
- Modify: `textbook/quizzes/REVIEW-07-1.md` (дописать раздел)

**Interfaces:**
- Consumes: формат Task 1, распределение Task 2.
- Produces: каждый файл параграфа содержит ≥3 вопросов; в банке появились
  типы `multi`, `number`, `text`, `match`, `order`.

- [ ] **Step 1: Составить 10–15 вопросов**

Требования: каждый параграф добрать до ≥3; суммарно покрыть пять типов
(`multi`, `number`, `text`, `match`, `order`); уровень — 7 класс, формулировки
на «ты» не нужны (вопросы безличные); `id` — `07-1.P-xNN` (P — параграф),
`source: "junior_it"`; `why` обязателен, `terms` — где есть термин.
Примеры направления (не обязательный список):

- `number` (§1.5): «Сколько байт в 2 Кбайт?» — answer 2048; «Сколько бит в
  одном байте?» — answer 8.
- `text` (§1.5): «Как называется наименьшая единица измерения информации?» —
  answer «бит».
- `match` (§1.1): виды информации по способу восприятия — пары
  «орган чувств → вид информации».
- `order` (§1.5): единицы измерения по возрастанию — бит, байт, Кбайт,
  Мбайт, Гбайт.
- `multi` (§1.2): «Отметь всё, что относится к информационным процессам».

Ответы и формулировки сверить с текстом соответствующего параграфа в OCR,
не по памяти.

- [ ] **Step 2: Прогнать валидатор — теперь строго зелёный**

Run: `python3 tools/validate_quizzes.py .`
Expected: exit 0, «банк валиден»

- [ ] **Step 3: Дописать REVIEW-07-1.md**

Раздел «Наши вопросы»: список id + на чём основан ответ (параграф/страница).

- [ ] **Step 4: Commit**

```bash
git add textbook/quizzes/
git commit -m "trainer: наши вопросы главы 1 — добор параграфов и редкие типы"
```

**Checkpoint после Task 3:** банк уходит Димасу на вычитку (сверка с бумажным
учебником). Вычитка не блокирует Tasks 4–11 — код не зависит от текстов
вопросов. Правки вычитки — отдельным коммитом, после него удалить
`REVIEW-07-1.md`.

---

### Task 4: engine.js — чистая логика

**Files:**
- Create: `trainer/js/engine.js`
- Test: `tools/trainer-tests/engine.test.mjs`

**Interfaces:**
- Produces (контракт для ui.js и Task 5–8):
  - `normalizeText(s: string): string` — trim, нижний регистр, `ё`→`е`,
    схлопывание пробелов.
  - `checkAnswer(question, answer): boolean` — answer по типам: single и
    find-error — int; multi — int[]; number — string|number; text — string;
    match — int[] (для каждого left — индекс right); order — int[]
    (индексы options в выбранном порядке).
  - `shuffle(items, rng): array` — копия, Фишер–Йетс; `rng(): number`
    в [0, 1).
  - `buildTicket(bank, rng, perParagraph = 3): question[]` — bank:
    `[{paragraph: "1.1", questions: [...]}, ...]`; из каждого параграфа
    perParagraph случайных вопросов, каждому добавлено поле `paragraph`,
    результат перемешан. Бросает Error, если в параграфе вопросов меньше.
  - `score(ticket, answers): {correct, total, percent, byParagraph, passed}` —
    answers: массив той же длины, `null`/`undefined` = нет ответа;
    `byParagraph`: `{"1.1": [верно, всего]}`; `passed = correct >=
    Math.ceil(total * 0.8)`.
  - `resultLine(attempt, name): string` — attempt:
    `{date: "2026-09-30", chapter: "07-1", size, correct, byParagraph,
    passed}`; формат: `Зачёт · 7 кл, глава 1 · 13/15 (87%) · сдано ·
    §1.1 3/3 · … · 30.09.2026 · Имя` (параграфы отсортированы; «пока нет»
    вместо «сдано»; имя-хвост опускается, если name пустой).

- [ ] **Step 1: Написать падающие тесты**

`tools/trainer-tests/engine.test.mjs`:

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import {
  normalizeText, checkAnswer, shuffle, buildTicket, score, resultLine,
} from '../../trainer/js/engine.js';

// Детерминированный rng: выдаёт значения по кругу.
const cycleRng = (values) => {
  let i = 0;
  return () => values[i++ % values.length];
};

const q = (over = {}) => ({
  id: '07-1-01', source: 'x', type: 'single', q: '?',
  options: ['а', 'б', 'в'], answer: 1, why: 'w', terms: [], ...over,
});

test('normalizeText: регистр, ё, пробелы', () => {
  assert.equal(normalizeText('  Полёт  Нормальный '), 'полет нормальный');
});

test('single: верно и неверно', () => {
  assert.equal(checkAnswer(q(), 1), true);
  assert.equal(checkAnswer(q(), 0), false);
});

test('find-error: как single', () => {
  assert.equal(checkAnswer(q({ type: 'find-error' }), 1), true);
});

test('multi: порядок выбора не важен, состав важен', () => {
  const question = q({ type: 'multi', answer: [0, 2] });
  assert.equal(checkAnswer(question, [2, 0]), true);
  assert.equal(checkAnswer(question, [0]), false);
  assert.equal(checkAnswer(question, [0, 1, 2]), false);
});

test('number: строка с запятой равна числу', () => {
  const question = q({ type: 'number', options: undefined, answer: 2048 });
  assert.equal(checkAnswer(question, '2048'), true);
  assert.equal(checkAnswer(question, ' 2048 '), true);
  assert.equal(checkAnswer(question, '2,5'), false);
  assert.equal(checkAnswer(q({ type: 'number', options: undefined, answer: 2.5 }), '2,5'), true);
});

test('text: нормализация и список допустимых', () => {
  const question = q({ type: 'text', options: undefined, answer: ['бит', 'bit'] });
  assert.equal(checkAnswer(question, '  БИТ '), true);
  assert.equal(checkAnswer(question, 'байт'), false);
});

test('match и order: точное совпадение последовательности', () => {
  const m = q({ type: 'match', options: { left: ['а', 'б'], right: ['x', 'y'] }, answer: [1, 0] });
  assert.equal(checkAnswer(m, [1, 0]), true);
  assert.equal(checkAnswer(m, [0, 1]), false);
  const o = q({ type: 'order', answer: [2, 0, 1] });
  assert.equal(checkAnswer(o, [2, 0, 1]), true);
  assert.equal(checkAnswer(o, [0, 1, 2]), false);
});

test('shuffle: не мутирует и детерминирован при данном rng', () => {
  const src = [1, 2, 3, 4];
  const out = shuffle(src, cycleRng([0]));
  assert.deepEqual(src, [1, 2, 3, 4]);
  assert.equal(out.length, 4);
  assert.deepEqual([...out].sort(), [1, 2, 3, 4]);
});

test('buildTicket: 3 с параграфа, поле paragraph, ошибка при нехватке', () => {
  const bank = [
    { paragraph: '1.1', questions: [q({ id: 'a1' }), q({ id: 'a2' }), q({ id: 'a3' }), q({ id: 'a4' })] },
    { paragraph: '1.2', questions: [q({ id: 'b1' }), q({ id: 'b2' }), q({ id: 'b3' })] },
  ];
  const ticket = buildTicket(bank, cycleRng([0.1, 0.5, 0.9]));
  assert.equal(ticket.length, 6);
  assert.equal(ticket.filter((t) => t.paragraph === '1.1').length, 3);
  assert.equal(ticket.filter((t) => t.paragraph === '1.2').length, 3);
  assert.throws(() => buildTicket([{ paragraph: '1.3', questions: [q()] }], cycleRng([0])));
});

test('score: порог 80 процентов с округлением вверх', () => {
  const ticket = Array.from({ length: 5 }, (_, i) =>
    ({ ...q({ id: `t${i}` }), paragraph: i < 3 ? '1.1' : '1.2' }));
  // 4 из 5 верных: ceil(5 * 0.8) = 4 — сдано
  const s = score(ticket, [1, 1, 1, 1, 0]);
  assert.equal(s.correct, 4);
  assert.equal(s.passed, true);
  assert.deepEqual(s.byParagraph, { '1.1': [3, 3], '1.2': [1, 2] });
  // 3 из 5 — не сдано; null = нет ответа
  assert.equal(score(ticket, [1, 1, 1, null, 0]).passed, false);
});

test('resultLine: формат строки для MAX', () => {
  const attempt = {
    date: '2026-09-30', chapter: '07-1', size: 15, correct: 13,
    byParagraph: { '1.2': [2, 3], '1.1': [3, 3] }, passed: true,
  };
  assert.equal(
    resultLine(attempt, 'Имя'),
    'Зачёт · 7 кл, глава 1 · 13/15 (87%) · сдано · §1.1 3/3 · §1.2 2/3 · 30.09.2026 · Имя',
  );
  assert.ok(resultLine({ ...attempt, passed: false }, '').endsWith('30.09.2026'));
  assert.ok(resultLine({ ...attempt, passed: false }, '').includes('пока нет'));
});
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `node --test tools/trainer-tests/`
Expected: FAIL — `Cannot find module .../trainer/js/engine.js`

- [ ] **Step 3: Реализовать `trainer/js/engine.js`**

```js
// Чистая логика тренажёра: без DOM, без fetch, без localStorage.
// rng передаётся параметром — в тестах детерминированный.

export function normalizeText(s) {
  return s.trim().toLowerCase().replaceAll('ё', 'е').replace(/\s+/g, ' ');
}

function sameSequence(a, b) {
  return a.length === b.length && a.every((v, i) => v === b[i]);
}

export function checkAnswer(question, answer) {
  switch (question.type) {
    case 'single':
    case 'find-error':
      return answer === question.answer;
    case 'multi': {
      const given = [...answer].sort((x, y) => x - y);
      const right = [...question.answer].sort((x, y) => x - y);
      return sameSequence(given, right);
    }
    case 'number':
      return Number(String(answer).trim().replace(',', '.')) === question.answer;
    case 'text': {
      const accepted = Array.isArray(question.answer) ? question.answer : [question.answer];
      return accepted.some((v) => normalizeText(v) === normalizeText(String(answer)));
    }
    case 'match':
    case 'order':
      return sameSequence(answer, question.answer);
    default:
      throw new Error(`неизвестный тип вопроса: ${question.type}`);
  }
}

export function shuffle(items, rng) {
  const arr = [...items];
  for (let i = arr.length - 1; i > 0; i -= 1) {
    const j = Math.floor(rng() * (i + 1));
    [arr[i], arr[j]] = [arr[j], arr[i]];
  }
  return arr;
}

export function buildTicket(bank, rng, perParagraph = 3) {
  const picked = [];
  for (const part of bank) {
    if (part.questions.length < perParagraph) {
      throw new Error(`в параграфе ${part.paragraph} меньше ${perParagraph} вопросов`);
    }
    for (const question of shuffle(part.questions, rng).slice(0, perParagraph)) {
      picked.push({ ...question, paragraph: part.paragraph });
    }
  }
  return shuffle(picked, rng);
}

export function score(ticket, answers) {
  const byParagraph = {};
  let correct = 0;
  ticket.forEach((question, i) => {
    const given = answers[i];
    const ok = given !== null && given !== undefined && checkAnswer(question, given);
    const bucket = byParagraph[question.paragraph] ?? (byParagraph[question.paragraph] = [0, 0]);
    bucket[1] += 1;
    if (ok) {
      bucket[0] += 1;
      correct += 1;
    }
  });
  const total = ticket.length;
  return {
    correct,
    total,
    percent: Math.round((correct / total) * 100),
    byParagraph,
    passed: correct >= Math.ceil(total * 0.8),
  };
}

export function resultLine(attempt, name) {
  const [grade, chapter] = attempt.chapter.split('-');
  const paragraphs = Object.keys(attempt.byParagraph).sort().map(
    (p) => `§${p} ${attempt.byParagraph[p][0]}/${attempt.byParagraph[p][1]}`,
  );
  const [y, m, d] = attempt.date.split('-');
  const pieces = [
    'Зачёт',
    `${Number(grade)} кл, глава ${Number(chapter)}`,
    `${attempt.correct}/${attempt.size} (${Math.round((attempt.correct / attempt.size) * 100)}%)`,
    attempt.passed ? 'сдано' : 'пока нет',
    ...paragraphs,
    `${d}.${m}.${y}`,
  ];
  if (name) {
    pieces.push(name);
  }
  return pieces.join(' · ');
}
```

- [ ] **Step 4: Прогнать тесты**

Run: `node --test tools/trainer-tests/`
Expected: PASS (12 тестов)

- [ ] **Step 5: Commit**

```bash
git add trainer/js/engine.js tools/trainer-tests/engine.test.mjs
git commit -m "trainer: движок — билет, проверка семи типов, скоринг, строка результата"
```

---

### Task 5: storage.js — localStorage и состояние

**Files:**
- Create: `trainer/js/storage.js`
- Test: `tools/trainer-tests/storage.test.mjs`

**Interfaces:**
- Produces (контракт для ui.js):
  - `loadState(storage): {name: string, attempts: Attempt[]}` — битый или
    отсутствующий JSON даёт `{name: '', attempts: []}`.
  - `saveName(state, name, storage): state` — новый объект состояния,
    записанный в storage.
  - `addAttempt(state, attempt, storage): state` — то же для попытки.
  - Attempt: `{date, chapter, size, correct, byParagraph, passed}` (как в
    engine.resultLine).
  - `storage` — любой объект с `getItem`/`setItem` (в бою
    `window.localStorage`, в тестах — обёртка над Map). Ключ: `jit-trainer`.

- [ ] **Step 1: Написать падающие тесты**

`tools/trainer-tests/storage.test.mjs`:

```js
import test from 'node:test';
import assert from 'node:assert/strict';
import { loadState, saveName, addAttempt } from '../../trainer/js/storage.js';

const fakeStorage = () => {
  const map = new Map();
  return {
    getItem: (k) => (map.has(k) ? map.get(k) : null),
    setItem: (k, v) => map.set(k, String(v)),
  };
};

test('loadState: пусто и битый JSON дают чистое состояние', () => {
  const s = fakeStorage();
  assert.deepEqual(loadState(s), { name: '', attempts: [] });
  s.setItem('jit-trainer', '{оборвано');
  assert.deepEqual(loadState(s), { name: '', attempts: [] });
});

test('saveName и addAttempt: пишут и читаются обратно', () => {
  const s = fakeStorage();
  let state = loadState(s);
  state = saveName(state, 'Имя', s);
  const attempt = {
    date: '2026-09-30', chapter: '07-1', size: 15, correct: 13,
    byParagraph: { '1.1': [3, 3] }, passed: true,
  };
  state = addAttempt(state, attempt, s);
  const reloaded = loadState(s);
  assert.equal(reloaded.name, 'Имя');
  assert.deepEqual(reloaded.attempts, [attempt]);
});
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `node --test tools/trainer-tests/`
Expected: FAIL — `Cannot find module .../trainer/js/storage.js`
(тесты engine из Task 4 остаются зелёными)

- [ ] **Step 3: Реализовать `trainer/js/storage.js`**

```js
const KEY = 'jit-trainer';

export function loadState(storage = window.localStorage) {
  try {
    const raw = storage.getItem(KEY);
    const parsed = raw ? JSON.parse(raw) : null;
    if (parsed && typeof parsed === 'object') {
      return {
        name: typeof parsed.name === 'string' ? parsed.name : '',
        attempts: Array.isArray(parsed.attempts) ? parsed.attempts : [],
      };
    }
  } catch {
    // битые данные не повод падать — начинаем с чистого состояния
  }
  return { name: '', attempts: [] };
}

function persist(state, storage) {
  storage.setItem(KEY, JSON.stringify(state));
  return state;
}

export function saveName(state, name, storage = window.localStorage) {
  return persist({ ...state, name }, storage);
}

export function addAttempt(state, attempt, storage = window.localStorage) {
  return persist({ ...state, attempts: [...state.attempts, attempt] }, storage);
}
```

- [ ] **Step 4: Прогнать тесты**

Run: `node --test tools/trainer-tests/`
Expected: PASS (engine + storage)

- [ ] **Step 5: Commit**

```bash
git add trainer/js/storage.js tools/trainer-tests/storage.test.mjs
git commit -m "trainer: хранение имени и попыток в localStorage"
```

---

### Task 6: Оболочка тренажёра — index.html, trainer.css, загрузка банка, стартовый экран

**Files:**
- Create: `trainer/index.html`
- Create: `trainer/trainer.css`
- Create: `trainer/js/ui.js`

**Interfaces:**
- Consumes: engine.js и storage.js (Task 4–5), банк (Task 2–3), манифест.
- Produces: рабочая страница со стартовым экраном; каркас функций для
  Task 7–8: `showScreen(id)`, `loadBank(): Promise<{meta, parts}>` (meta —
  запись банка из манифеста, parts — `[{paragraph, title, questions}]`),
  глобальное состояние `app = {state, bank, mode, scope, ticket, current,
  answers, queue}`.

- [ ] **Step 1: Разметка `trainer/index.html`**

Три секции-экрана, видимость через класс `active`. Скелет:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Тренажёр · Junior_IT</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@700&family=Manrope:wght@400;500;600;700&family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="trainer.css">
</head>
<body>
  <header class="bar">
    <a class="home" href="../">Junior_IT</a>
    <span class="chip">BOOK</span>
  </header>

  <main>
    <section id="screen-start" class="screen active">
      <h1>Тренажёр</h1>
      <p class="bank-title" id="bank-title"></p>
      <label class="name-row">Как тебя зовут?
        <input id="student-name" type="text" maxlength="30" autocomplete="off">
      </label>
      <fieldset class="scope" id="scope-box">
        <legend>Что тренируем</legend>
        <!-- радио «вся глава» + по одному на параграф, генерирует ui.js -->
      </fieldset>
      <div class="mode-buttons">
        <button id="btn-training" class="big">Тренировка</button>
        <button id="btn-exam" class="big accent">Зачёт</button>
      </div>
      <p class="hint">Зачёт — всегда по всей главе: билет из 15 вопросов,
        по три с каждого параграфа. Сдано — от 12 верных.</p>
      <div id="attempts" class="attempts"></div>
    </section>

    <section id="screen-question" class="screen">
      <div class="progress"><span id="progress-label"></span>
        <div class="progress-track"><div id="progress-fill"></div></div>
      </div>
      <article class="card">
        <p class="source" id="q-source"></p>
        <h2 id="q-text"></h2>
        <div id="q-body"></div>
        <div id="q-why" class="why" hidden></div>
        <button id="btn-answer" class="big accent">Ответить</button>
        <button id="btn-next" class="big" hidden>Дальше</button>
      </article>
    </section>

    <section id="screen-result" class="screen">
      <h1 id="result-verdict"></h1>
      <p class="score-line" id="result-score"></p>
      <ul id="result-breakdown"></ul>
      <button id="btn-copy" class="big accent">Скопировать результат</button>
      <button id="btn-again" class="big">Ещё раз</button>
      <p class="hint">Скопированную строку отправь в MAX.</p>
    </section>
  </main>

  <footer><p>Junior_IT · тренажёр по учебнику</p></footer>
  <script type="module" src="js/ui.js"></script>
</body>
</html>
```

- [ ] **Step 2: Стили `trainer/trainer.css`**

Вставить блок токенов из `meta/brand-tokens.css` в начало файла (`:root`
целиком — материалы самодостаточны, комментарий канона сохранить). Дальше —
стили тренажёра: тёмный фон `--bg`, карточки `--card` со скруглением 16px и
мягкой тенью, акцент `--track-book` (кнопка `.accent`, полоса прогресса,
рамка выбранного варианта), `body` — Manrope, `h1/h2` — Rubik 700, `.chip` и
`.source` — JetBrains Mono. Кнопки `.big` — min-height 56px, на всю ширину
карточки. Варианты ответов — крупные кликабельные `label` с padding ≥14px.
Плашка `.why` — фон `--card-2`, полоса `--track-book` слева. Верно/неверно —
рамка `--green` / `--orange` (не красный: мягче для ребёнка). Адаптив: одна
колонка, `max-width: 720px` по центру, без `position:absolute`.

- [ ] **Step 3: `trainer/js/ui.js` — загрузка и стартовый экран**

```js
import { buildTicket, checkAnswer, score, resultLine, shuffle } from './engine.js';
import { loadState, saveName, addAttempt } from './storage.js';

const BANK_BASE = '../textbook/quizzes/';
const BANK_ID = '07-1';

const app = {
  state: loadState(),
  bank: null,     // {meta, parts: [{paragraph, title, questions}]}
  mode: null,     // 'training' | 'exam'
  scope: 'all',   // 'all' | '1.1' | ...
  ticket: [],
  current: 0,
  answers: [],
  queue: [],      // тренировка: индексы вопросов к показу
};

export function showScreen(id) {
  document.querySelectorAll('.screen').forEach((s) => s.classList.remove('active'));
  document.getElementById(id).classList.add('active');
}

async function loadBank() {
  const manifest = await (await fetch(`${BANK_BASE}index.json`)).json();
  const meta = manifest.banks.find((b) => b.id === BANK_ID);
  const parts = await Promise.all(meta.files.map(async (name) => {
    const data = await (await fetch(BANK_BASE + name)).json();
    return { paragraph: data.paragraph, title: data.title, questions: data.questions };
  }));
  return { meta, parts };
}

function renderScope(parts) {
  const box = document.getElementById('scope-box');
  const options = [{ value: 'all', label: 'Вся глава' }].concat(
    parts.map((p) => ({ value: p.paragraph, label: `§${p.paragraph} ${p.title}` })),
  );
  for (const opt of options) {
    const label = document.createElement('label');
    const input = Object.assign(document.createElement('input'),
      { type: 'radio', name: 'scope', value: opt.value, checked: opt.value === 'all' });
    input.addEventListener('change', () => { app.scope = opt.value; });
    label.append(input, ` ${opt.label}`);
    box.append(label);
  }
}

function renderAttempts() {
  const box = document.getElementById('attempts');
  const last = app.state.attempts.slice(-3).reverse();
  box.innerHTML = last.length ? '<h3>Прошлые зачёты</h3>' : '';
  for (const a of last) {
    const p = document.createElement('p');
    p.className = a.passed ? 'attempt ok' : 'attempt';
    p.textContent = resultLine(a, '');
    box.append(p);
  }
}

async function init() {
  app.bank = await loadBank();
  document.getElementById('bank-title').textContent =
    `7 класс · глава ${app.bank.meta.chapter}. ${app.bank.meta.title}`;
  const nameInput = document.getElementById('student-name');
  nameInput.value = app.state.name;
  nameInput.addEventListener('change', () => {
    app.state = saveName(app.state, nameInput.value.trim());
  });
  renderScope(app.bank.parts);
  renderAttempts();
  document.getElementById('btn-training').addEventListener('click', () => startTraining());
  document.getElementById('btn-exam').addEventListener('click', () => startExam());
}

function startTraining() { /* Task 7 */ }
function startExam() { /* Task 7–8 */ }

init();
```

(Заглушки `startTraining`/`startExam` — это границы Task 7–8, единственное
допустимое «позже» в этом плане: без них страница не соберётся.)

- [ ] **Step 4: Ручная проверка**

Run: `python3 -m http.server 8000` из корня → открыть
`http://localhost:8000/trainer/`.
Проверить: стартовый экран в бренд-теме, заголовок банка подтянулся из
манифеста, охваты перечислены (§1.1–1.5), имя сохраняется после перезагрузки
страницы, консоль без ошибок.

- [ ] **Step 5: Commit**

```bash
git add trainer/
git commit -m "trainer: оболочка — стартовый экран, загрузка банка, стили"
```

---

### Task 7: Экран вопроса — рендер типов и Тренировка

**Files:**
- Modify: `trainer/js/ui.js` (заполнить startTraining, добавить рендеры)
- Modify: `trainer/trainer.css` (стили вариантов, match/order-контролов)

**Interfaces:**
- Consumes: `checkAnswer`, `shuffle` из engine.js; каркас Task 6.
- Produces: `renderQuestion(question)` — рисует тело вопроса в `#q-body`;
  `readAnswer(question): answer|null` — снимает ответ в формате
  `checkAnswer` (null = ответ не дан); Тренировка целиком.

- [ ] **Step 1: Рендеры по типам**

В `ui.js`:

```js
function renderQuestion(question) {
  document.getElementById('q-source').textContent =
    question.source === 'junior_it' ? 'Junior_IT' : question.source;
  document.getElementById('q-text').textContent = question.q;
  const body = document.getElementById('q-body');
  body.innerHTML = '';
  body.dataset.type = question.type;

  if (question.type === 'single' || question.type === 'multi' || question.type === 'find-error') {
    const inputType = question.type === 'multi' ? 'checkbox' : 'radio';
    question.options.forEach((text, i) => {
      const label = document.createElement('label');
      label.className = question.type === 'find-error' ? 'option code' : 'option';
      const input = Object.assign(document.createElement('input'),
        { type: inputType, name: 'opt', value: String(i) });
      label.append(input, ` ${text}`);
      body.append(label);
    });
  } else if (question.type === 'number' || question.type === 'text') {
    const input = Object.assign(document.createElement('input'), {
      type: 'text', id: 'free-answer', autocomplete: 'off',
      inputMode: question.type === 'number' ? 'numeric' : 'text',
    });
    body.append(input);
    input.focus();
  } else if (question.type === 'match') {
    question.options.left.forEach((leftText, i) => {
      const row = document.createElement('label');
      row.className = 'match-row';
      const select = document.createElement('select');
      select.dataset.row = String(i);
      select.append(new Option('—', ''));
      question.options.right.forEach((rightText, j) =>
        select.append(new Option(rightText, String(j))));
      row.append(`${leftText} `, select);
      body.append(row);
    });
  } else if (question.type === 'order') {
    const list = document.createElement('ol');
    list.id = 'order-list';
    shuffle(question.options.map((text, i) => ({ text, i })), Math.random)
      .forEach(({ text, i }) => {
        const item = document.createElement('li');
        item.dataset.index = String(i);
        const up = Object.assign(document.createElement('button'), { textContent: '↑', type: 'button' });
        const down = Object.assign(document.createElement('button'), { textContent: '↓', type: 'button' });
        up.addEventListener('click', () => item.previousElementSibling && list.insertBefore(item, item.previousElementSibling));
        down.addEventListener('click', () => item.nextElementSibling && list.insertBefore(item.nextElementSibling, item));
        item.append(Object.assign(document.createElement('span'), { textContent: text }), up, down);
        list.append(item);
      });
    body.append(list);
  }
}

function readAnswer(question) {
  const body = document.getElementById('q-body');
  switch (question.type) {
    case 'single':
    case 'find-error': {
      const checked = body.querySelector('input[name="opt"]:checked');
      return checked ? Number(checked.value) : null;
    }
    case 'multi': {
      const checked = [...body.querySelectorAll('input[name="opt"]:checked')];
      return checked.length ? checked.map((c) => Number(c.value)) : null;
    }
    case 'number':
    case 'text': {
      const value = document.getElementById('free-answer').value.trim();
      return value ? value : null;
    }
    case 'match': {
      const values = [...body.querySelectorAll('select')].map((s) => s.value);
      return values.every((v) => v !== '') ? values.map(Number) : null;
    }
    case 'order':
      return [...body.querySelectorAll('#order-list li')].map((li) => Number(li.dataset.index));
    default:
      return null;
  }
}
```

`.option.code` в CSS — JetBrains Mono на фоне `#15151c` (консольная палитра
уроков; для будущих find-error).

- [ ] **Step 2: Тренировка**

```js
function trainingPool() {
  const parts = app.scope === 'all'
    ? app.bank.parts
    : app.bank.parts.filter((p) => p.paragraph === app.scope);
  return shuffle(
    parts.flatMap((p) => p.questions.map((q) => ({ ...q, paragraph: p.paragraph }))),
    Math.random,
  );
}

function startTraining() {
  app.mode = 'training';
  app.ticket = trainingPool();
  app.queue = app.ticket.map((_, i) => i);
  showScreen('screen-question');
  showTrainingQuestion();
}

function showTrainingQuestion() {
  if (!app.queue.length) {
    showScreen('screen-start');
    renderAttempts();
    return;
  }
  const question = app.ticket[app.queue[0]];
  document.getElementById('progress-label').textContent = `Осталось: ${app.queue.length}`;
  document.getElementById('progress-fill').style.width =
    `${Math.round((1 - app.queue.length / app.ticket.length) * 100)}%`;
  renderQuestion(question);
  const why = document.getElementById('q-why');
  why.hidden = true;
  toggleButtons({ answer: true, next: false });
  document.getElementById('btn-answer').onclick = () => {
    const answer = readAnswer(question);
    if (answer === null) return; // ответа нет — кнопка молчит
    const ok = checkAnswer(question, answer);
    markAnswer(ok);
    why.textContent = question.why;
    why.hidden = false;
    if (ok) {
      app.queue.shift();
    } else {
      app.queue.push(app.queue.shift()); // неверный — в конец очереди
    }
    toggleButtons({ answer: false, next: true });
    document.getElementById('btn-next').onclick = () => showTrainingQuestion();
  };
}

function toggleButtons({ answer, next }) {
  document.getElementById('btn-answer').hidden = !answer;
  document.getElementById('btn-next').hidden = !next;
}

function markAnswer(ok) {
  const card = document.querySelector('#screen-question .card');
  card.classList.remove('right', 'wrong');
  card.classList.add(ok ? 'right' : 'wrong');
}
```

`markAnswer` сбрасывать при рендере следующего вопроса
(`card.classList.remove('right', 'wrong')` в начале `renderQuestion` или
`showTrainingQuestion`). Инпуты после ответа блокировать
(`body.querySelectorAll('input,select,button').forEach((el) => { el.disabled = true; })`
внутри обработчика «Ответить»).

- [ ] **Step 3: Ручная проверка**

`http://localhost:8000/trainer/` → Тренировка, вся глава: каждый тип вопроса
рендерится и отвечается; неверный ответ показывает `why` и возвращается в
конец; счётчик «Осталось» тает; охват «§1.5» даёт только вопросы 1.5;
прохождение до конца возвращает на старт. Консоль чистая.

- [ ] **Step 4: Commit**

```bash
git add trainer/
git commit -m "trainer: рендер типов вопросов и режим Тренировка"
```

---

### Task 8: Зачёт и экран итога

**Files:**
- Modify: `trainer/js/ui.js` (startExam, итог, копирование)
- Modify: `trainer/trainer.css` (стили итога, если чего-то не хватает)

**Interfaces:**
- Consumes: `buildTicket`, `score`, `resultLine`, `addAttempt`; рендеры Task 7.
- Produces: полный цикл Зачёта с записью попытки и копированием строки.

- [ ] **Step 1: Зачёт**

```js
function startExam() {
  app.mode = 'exam';
  app.ticket = buildTicket(app.bank.parts, Math.random);
  app.answers = new Array(app.ticket.length).fill(null);
  app.current = 0;
  showScreen('screen-question');
  showExamQuestion();
}

function showExamQuestion() {
  const question = app.ticket[app.current];
  document.getElementById('progress-label').textContent =
    `Вопрос ${app.current + 1} из ${app.ticket.length}`;
  document.getElementById('progress-fill').style.width =
    `${Math.round((app.current / app.ticket.length) * 100)}%`;
  renderQuestion(question);
  document.getElementById('q-why').hidden = true; // в зачёте why не показываем
  toggleButtons({ answer: true, next: false });
  const btn = document.getElementById('btn-answer');
  btn.textContent = app.current + 1 === app.ticket.length ? 'Ответить и закончить' : 'Ответить';
  btn.onclick = () => {
    const answer = readAnswer(question);
    if (answer === null) return;
    app.answers[app.current] = answer;
    app.current += 1;
    if (app.current < app.ticket.length) {
      showExamQuestion();
    } else {
      finishExam();
    }
  };
}

function finishExam() {
  const result = score(app.ticket, app.answers);
  const attempt = {
    date: new Date().toISOString().slice(0, 10),
    chapter: BANK_ID,
    size: result.total,
    correct: result.correct,
    byParagraph: result.byParagraph,
    passed: result.passed,
  };
  app.state = addAttempt(app.state, attempt);
  document.getElementById('result-verdict').textContent =
    result.passed ? 'Сдано' : 'Пока нет';
  document.getElementById('result-score').textContent =
    `${result.correct} из ${result.total} (${result.percent}%)`;
  const list = document.getElementById('result-breakdown');
  list.innerHTML = '';
  for (const p of Object.keys(result.byParagraph).sort()) {
    const [ok, total] = result.byParagraph[p];
    const item = document.createElement('li');
    item.textContent = `§${p}: ${ok} из ${total}`;
    list.append(item);
  }
  const line = resultLine(attempt, app.state.name);
  document.getElementById('btn-copy').onclick = async () => {
    await navigator.clipboard.writeText(line);
    document.getElementById('btn-copy').textContent = 'Скопировано';
  };
  document.getElementById('btn-copy').textContent = 'Скопировать результат';
  document.getElementById('btn-again').onclick = () => {
    showScreen('screen-start');
    renderAttempts();
  };
  showScreen('screen-result');
}
```

Кнопку «Ответить и закончить» текстом возвращать в «Ответить» при следующем
запуске зачёта (первая строка `showExamQuestion` это делает через
`btn.textContent`).

- [ ] **Step 2: Ручная проверка**

Полный зачёт: 15 вопросов, по 3 с параграфа (проверить по `q-source` /
разбивке итога); вернуться назад нельзя; после последнего — итог с
процентом и разбивкой; «Скопировать» кладёт строку формата из спеки;
попытка появилась в «Прошлых зачётах» на старте и переживает перезагрузку;
второй запуск зачёта даёт другой билет. Порог: намеренно ответить неверно
на 4 вопроса → «Пока нет» при 11/15, «Сдано» при 12/15.

- [ ] **Step 3: Прогнать все автотесты (регрессия)**

Run: `node --test tools/trainer-tests/ && python3 tools/validate_quizzes.py .`
Expected: PASS / exit 0

- [ ] **Step 4: Commit**

```bash
git add trainer/
git commit -m "trainer: режим Зачёт — билет, итог, строка для MAX"
```

---

### Task 9: build_site.sh — белый список

**Files:**
- Modify: `tools/build_site.sh` (переписать секцию rsync)
- Modify: `tools/tests/test_build_site.py` (обновить ожидания)

**Interfaces:**
- Produces: `_site/` содержит ТОЛЬКО перечисленное; `trainer/` и
  `textbook/quizzes/` публикуются.

- [ ] **Step 1: Снять эталон текущей сборки**

```bash
./tools/build_site.sh /tmp/site-before && (cd /tmp/site-before && find . -maxdepth 1 | sort > /tmp/before.txt)
```

- [ ] **Step 2: Обновить тесты**

В `tools/tests/test_build_site.py` (прочитать текущие тесты, адаптировать):
добавить/поправить проверки — в сборке есть `trainer/index.html`,
`textbook/quizzes/index.json`, `docs/`, `lessons/`, `index.html`; в сборке
НЕТ `CLAUDE.md`, `pyrightconfig.json`, `meta/`, `teacher/`-каталогов внутри
`tracks/`, `docs/superpowers/`, `textbook/quizzes/REVIEW-07-1.md` — этот
файл добавить в исключения rsync до конца вычитки.

- [ ] **Step 3: Убедиться, что новые тесты падают**

Run: `cd tools && .venv/bin/python -m pytest tests/test_build_site.py -v`
Expected: FAIL на новых ожиданиях (CLAUDE.md сейчас публикуется)

- [ ] **Step 4: Переписать rsync-секцию на белый список**

```bash
# Белый список публикации: на сайт попадает только перечисленное.
PUBLISH=(
  index.html
  README.md
  LICENSE
  CHANGELOG.md
  CODE_OF_CONDUCT.md
  CONTRIBUTING.md
  SECURITY.md
  docs
  lessons
  textbook
  tracks
  trainer
)

SRC=()
for item in "${PUBLISH[@]}"; do
  SRC+=("$ROOT/$item")
done

rsync -a \
  --exclude 'teacher/' \
  --exclude 'live-code.md' \
  --exclude 'docs/superpowers/' \
  --exclude 'REVIEW-07-1.md' \
  --exclude '.DS_Store' \
  --exclude '__pycache__/' \
  "${SRC[@]}" "$OUT/"
```

Шапку скрипта (`rm -rf "$OUT"`, `mkdir`, вызов `build_course_map.py`) не
трогать. Комментарий в шапке обновить: «Белый список: публикуется только
перечисленное в PUBLISH».

- [ ] **Step 5: Прогнать тесты и сравнить сборки**

Run: `cd tools && .venv/bin/python -m pytest tests/test_build_site.py -v`
Expected: PASS

```bash
./tools/build_site.sh /tmp/site-after && (cd /tmp/site-after && find . -maxdepth 1 | sort > /tmp/after.txt)
diff /tmp/before.txt /tmp/after.txt
```
Expected: пропали только `CLAUDE.md` и `pyrightconfig.json`, появился
непустой `trainer/`. Любая другая разница — разобраться до коммита.

- [ ] **Step 6: Commit**

```bash
git add tools/build_site.sh tools/tests/test_build_site.py
git commit -m "build: белый список публикации вместо чёрного"
```

---

### Task 10: course-map.json — дата прохождения модуля

**Files:**
- Modify: `tools/build_course_map.py`
- Modify: `tools/tests/test_build_course_map.py`

**Interfaces:**
- Produces: у каждого модуля в `course-map.json` поле `completed_on`:
  ISO-дата первого занятия, где модуль завершён, или `null`. Контракт для
  будущей хронологии «свежих шпаргалок» на лендинге (сама хронология — вне
  этого плана).

- [ ] **Step 1: Падающий тест**

В `tools/tests/test_build_course_map.py` (использовать существующие
`make_module`/`make_session`, `TODAY = date(2026, 9, 12)`):

```python
def test_completed_on_from_first_completing_session(tmp_path):
    make_module(tmp_path, "book/m-01-info")
    make_module(tmp_path, "python/m-01-run")
    make_session(tmp_path, date(2026, 9, 1), ["book/m-01-info"])
    make_session(tmp_path, date(2026, 9, 8), ["book/m-01-info", "python/m-01-run"],
                 completed=["python/m-01-run"])
    course_map = build_course_map(tmp_path, TODAY)
    by_id = {m["id"]: m for m in course_map["modules"]}
    assert by_id["book/m-01-info"]["completed_on"] == "2026-09-01"
    assert by_id["python/m-01-run"]["completed_on"] == "2026-09-08"


def test_completed_on_none_for_unfinished(tmp_path):
    make_module(tmp_path, "book/m-02-next")
    course_map = build_course_map(tmp_path, TODAY)
    assert course_map["modules"][0]["completed_on"] is None
```

- [ ] **Step 2: Убедиться, что тесты падают**

Run: `cd tools && .venv/bin/python -m pytest tests/test_build_course_map.py -v`
Expected: FAIL — `KeyError: 'completed_on'`

- [ ] **Step 3: Реализовать**

В `build_course_map.py` добавить рядом с `module_statuses`:

```python
def completion_dates(sessions, today):
    """id модуля -> дата первого прошедшего занятия, где он завершён."""
    dates = {}
    for session in sessions:
        if session["date"] >= today:
            continue
        done_ids = (session["completed"] or []) if "completed" in session \
            else _playlist_ids(session)
        for mid in done_ids:
            dates.setdefault(mid, session["date"])
    return dates
```

В `build_course_map`: `dates = completion_dates(sessions, today)`, в
`out_modules.append({...})` добавить строку:

```python
            "completed_on": dates[m["id"]].isoformat() if m["id"] in dates else None,
```

Сессии уже отсортированы по дате в `load_sessions` — «первое занятие»
обеспечивается `setdefault`.

- [ ] **Step 4: Прогнать тесты**

Run: `cd tools && .venv/bin/python -m pytest tests/test_build_course_map.py -v`
Expected: PASS (новые и старые)

- [ ] **Step 5: Commit**

```bash
git add tools/build_course_map.py tools/tests/test_build_course_map.py
git commit -m "course-map: дата прохождения модуля в контракте"
```

---

### Task 11: CI — валидация банка и тесты движка перед сборкой

**Files:**
- Modify: `.github/workflows/deploy.yml`

**Interfaces:**
- Consumes: `tools/validate_quizzes.py` (Task 1), `tools/trainer-tests/`
  (Task 4–5). Node на ubuntu-latest предустановлен (v20+, `node --test`
  доступен с v18) — новых actions не добавляем.

- [ ] **Step 1: Добавить шаги в deploy.yml**

После шага `Checkout`, перед `Setup Pages`:

```yaml
      - name: Validate quiz bank
        run: python3 tools/validate_quizzes.py .
      - name: Trainer engine tests
        run: node --test tools/trainer-tests/
```

- [ ] **Step 2: Локальная репетиция CI-шагов**

Run: `python3 tools/validate_quizzes.py . && node --test tools/trainer-tests/`
Expected: оба зелёные (системный python3 без venv — валидатор на stdlib,
это и проверяем)

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "ci: валидация банка и тесты движка перед сборкой сайта"
```

---

### Task 12: Лендинг, brand-check и финальный смоук

**Files:**
- Modify: `index.html` (строка ~530: плашка «Тренажёр — скоро» → ссылка)

**Interfaces:**
- Consumes: рабочий `trainer/` (Task 6–8).

- [ ] **Step 1: Заменить плашку ссылкой**

В блоке `.hub-links`:

```html
        <a class="setup-link" href="trainer/">Тренажёр →</a>
```

вместо `<span class="stub-plate">Тренажёр — скоро</span>`. Если класс
`.stub-plate` больше нигде не используется (проверить grep-ом по
`index.html`) — удалить его CSS-правило из `<style>` лендинга.

- [ ] **Step 2: Полный локальный прогон**

```bash
cd tools && .venv/bin/python -m pytest && cd ..
node --test tools/trainer-tests/
python3 tools/validate_quizzes.py .
./tools/build_site.sh /tmp/site-smoke
```
Expected: всё зелёное. Затем `python3 -m http.server 8000 --directory /tmp/site-smoke`
→ пройти по цепочке: лендинг → ссылка «Тренажёр» → тренировка §1.5 (3
вопроса) → зачёт до итога → копирование строки. Это смоук именно собранного
сайта, не рабочей копии.

- [ ] **Step 3: brand-check**

Прогнать агента brand-guardian по `trainer/` и изменённому `index.html`
(правила: токены, шрифты с кириллицей, запрещённая лексика). Замечания
исправить до коммита.

- [ ] **Step 4: Commit**

```bash
git add index.html
git commit -m "landing: плашка тренажёра заменена живой ссылкой"
```

- [ ] **Step 5: Финал**

Сообщить Димасу: готово к `/deploy` (диff и резюме покажет сам скилл);
напомнить про вычитку банка (checkpoint Task 3) и удаление
`REVIEW-07-1.md` после неё (вместе с исключением из `build_site.sh`,
Task 9).
