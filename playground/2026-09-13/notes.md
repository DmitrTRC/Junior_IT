# Заметки занятия 13.09

Заполняется по ходу занятия и сразу после — не заранее. Здесь фиксируется
только то, что реально произошло, не план (план — в `session.yml`).

> **Если время поехало:** план отсечения — в `session.yml` →
> `cutting_plan`. 5 мин отставания → режем проверку Python в devops
> (шаг 6). 10 мин → плюс Windows-слайд и вступление/итог python-01.
> Traceback в python-01 (шаг 5) и живой код devops (шаги 1–5) — не
> трогать никогда.

## Что успели

## Где залипли

## Её вопросы

## Что перенести в модуль
